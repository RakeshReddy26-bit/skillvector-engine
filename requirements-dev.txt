-r requirements.txt

# ML/NLP (local dev + CI only — too large for Render free tier)
sentence-transformers>=2.2.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0

pytest>=7.4.0,<9.0.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0
ruff>=0.1.0
mypy>=1.7.0
pre-commit>=3.6.0
httpx>=0.25.0

# Streamlit (local dev only — not deployed to production)
streamlit>=1.30.0,<2.0.0
streamlit-option-menu>=0.3.6,<1.0.0
plotly>=5.18.0,<6.0.0
